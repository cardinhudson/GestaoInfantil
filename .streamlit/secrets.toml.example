# Exemplo de secrets para uso local (NUNCA comite credenciais reais).
# Copie este arquivo para `.streamlit/secrets.toml` e preencha com suas credenciais.
[database]
"""
Copie este arquivo para .streamlit/secrets.toml (não commitar) e ajuste.
Preencha GESTAO_DB com sua URL Postgres/Supabase (substitua host/usuario/senha).
Se a senha contiver @, encode como %40.
"""

# Exemplo (Troque host/usuario/senha):
# GESTAO_DB = "postgres://usuario:senha@host:5432/postgres"
# Se a senha tiver @, use %40. Ex.: senha "Hu121100@" fica "Hu121100%40".
[smtp]
server = "smtp.exemplo.com"  # servidor SMTP
port = 587                    # 587 para STARTTLS, 465 para SSL
user = "usuario@exemplo.com" # usuário/login
password = "sua_senha"      # senha
from = "app@seu-dominio.com" # remetente (From)
# use_ssl = true              # opcional: true para SMTPS (porta 465). Caso false, será usado STARTTLS (porta 587)

# Opcional: se você quiser log de testes, adicione:
# log_email_tests = true
