[server]
# headless = false: abre o navegador automaticamente ao rodar localmente
# No Streamlit Cloud, isso é ignorado automaticamente
headless = false
address = "localhost"
port = 8501
enableCORS = true

[browser]
gatherUsageStats = false
serverAddress = "localhost"
serverPort = 8501

