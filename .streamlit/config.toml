[server]

# Cloud-safe defaults. Streamlit Cloud exige headless e bind em 0.0.0.0.
headless = true
port = 8501
address = "0.0.0.0"
enableCORS = true

[browser]
# Evita prompt de onboarding/telemetria no startup.
gatherUsageStats = false

[global]
# Best-effort: evitar prompts interativos de onboarding
# hideOnboarding = true  # Removido: opção inválida
